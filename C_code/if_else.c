#include <stdio.h>
 
// if else statement
int main () {
   int a = 11;

   if ( a > 10 ) {
      printf( "if block is being used, a = %d \n", a );
   } else { 
      printf( "else block is being used, a = %d \n", a );
   }
 
   return 0;
}
